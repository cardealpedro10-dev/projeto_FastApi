[tool.poetry]
name = "fastapi-app"
version = "0.1.0"
description = "FastAPI com Docker e Poetry"
authors = ["Desenvolvedor <dev@exemplo.com>"]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.110.0"
uvicorn = {extras = ["standard"], version = "^0.28.0"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
